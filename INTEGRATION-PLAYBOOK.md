# 接入手册（给 AI 执行的版本）

> **这份文件的用法**：把一个项目接入 jjpay 时，让执行任务的 AI **整份读完**本文件，
> 按节次顺序执行，每节末尾的「验收」全绿才进下一节。
>
> 与 [README.md](README.md) 的分工：README 讲**这套 API 怎么调**（签名、结构体、错误码）；
> 本文件讲**在一个已经有支付逻辑的项目里，怎么把它接进去而不出事**——
> 顺序、决策点、以及两次真实接入（一次是"并列新增一个渠道"，一次是"全量替换直连渠道"）
> 踩出来的坑。两份都要读，本文件不重复 README 已写清的 API 细节。

---

## 0 · 元规则

| 标记 | 含义 |
|---|---|
| **必须** | 漏了会丢钱、错账或重复发货。没有例外，没有"这个项目情况特殊" |
| **禁止** | 写出来就是 bug |
| **决策** | 没有唯一正确答案。**必须显式选一个，并把理由写进 commit 正文**，不许默认 |

三条贯穿全文的元规则：

1. **先勘察，后写码。** 第 1 节的问题没答完，不准动第一行代码。这套接入的所有事故都
   源于"没搞清既有系统就开始写"，不是源于 API 用错。
2. **冲突时，钱的规则优先。** 本文件与项目既有约定冲突时：涉及金额、状态机、幂等、
   退款去向的按本文件；命名、分层、日志格式、提交规范按项目自己的 `CLAUDE.md` / `AGENTS.md`。
3. **抄规则，不抄代码。** 本文件的示例是最小形，照搬分层会和项目既有结构打架。

**版本下限：`v0.1.6`**。低于它的版本把 `BaseURL` 里的路径前缀也签进了待签串，
一旦网关按前缀分流并剥掉前缀回源，商户与网关两边算出的 PATH 不一致，表现是**每一次调用
都 20002 鉴权失败**（见附录 A-1）。新接入一律用最新版。

---

## 1 · 第 0 步：勘察（输出一份方案，等人确认后再动手）

下面每一项都要在项目代码里**找到证据**再回答，不许猜。回答完，用一段话向用户复述
"我打算怎么接"，**等确认**——这一步花十分钟，能省掉后面整轮返工。

| # | 要查清的事 | 为什么 | 答案决定了 |
|---|---|---|---|
| 1 | 这次是**并列新增**一个渠道，还是**全量替换**已有的直连渠道？ | 两种接法的约束完全不同 | 见下方 §1.1 / §1.2 |
| 2 | 项目里金额怎么存？（`decimal(10,2)` 的元／整数分／float） | SDK 只收**分**（`int64`），换算错一位就是错账 | 第 3 节的换算策略 |
| 3 | 商户单号（`out_trade_no`）取哪个字段？它**跨渠道是否共用号段**？ | 共用号段时，一条合法通知能去结算另一渠道的单 | 第 5 节的"渠道闸" |
| 4 | 单号在**本项目内**是否真的唯一、且**不复用**？ | 它同时是网关的幂等键 | 是否要另造号 |
| 5 | 现有退款是**同步语义**（调完就地翻状态）还是异步？ | 网关退款是异步的，落差要显式处理 | 第 6 节的**决策** |
| 6 | 现有回调链路有没有**比对金额**？有没有**幂等**？ | 老链路常常两样都没有，不要照抄它 | 第 5 节的三道闸 |
| 7 | "待支付订单"会不会占住别的闸？（注销预检、每人限一单、库存占用…） | 下单失败留下废单会锁死用户 | 第 4 节"失败不落库" |
| 8 | 前端有没有按 PC/移动端、按渠道分流的跳转逻辑？ | 网关只返回一个收银台链接，那些分流全部作废 | 第 8 节的删除清单 |
| 9 | 本地订单超时是多久？和网关的 `ExpireMinutes` 谁先到？ | 两边不收敛会出现"幂等命中一张已关掉的单" | 第 4 节 R4 |
| 10 | 项目有没有告警通道（飞书/钉钉/邮件）？ | 退款失败与卡单**必须**有人看见 | 第 6 节 |

### 1.1 并列新增（推荐的默认接法）

新渠道与既有的直连渠道**并列，不是取代**。硬约束：

- **必须**：历史订单的退款一律回**原路**。退款走哪条链路，只能由**订单上记着的那个渠道**
  决定，**禁止**重新走一遍"当前启用的是哪个渠道"的路由——那会把钱退到另一个商户号。
- **必须**：给这条不变量配一条回归护栏测试（构造一张直连渠道的历史订单，断言它的退款
  **不会**被改道到网关）。这是整个接入里最值得写的一个测试。
- **必须**：新渠道的开关默认**关闭**。没配置时，下单/退款直接报错、回调端点一律 401，
  既有链路零影响——这样接入代码可以先合并再灰度。
- 查单、对账、结果轮询这些按订单渠道分流的地方，**逐个**找出来加分支；老前端不传渠道
  参数时，后端要能自己回查订单的渠道。

### 1.2 全量替换（把直连渠道下线）

- **必须**：前后端**同一个 commit**。后端先收窄了枚举而前端还在传老渠道值，用户点一下
  就是 400。
- **必须**：只收窄**新下单**的入口。历史订单的退款、查单、回调路径**一条都不能删**，
  至少留到所有历史订单过了退款期。
- **必须**：待支付的老订单不能被锁死——用户用老渠道下过单还没付的，要能用新渠道付完
  （通常是"每次发起支付都覆写订单上的渠道字段"）。
- 顺带能删掉的复杂度（渠道选择交给收银台之后就不再需要）：按 PC/移动端分流的跳转、
  自绘的扫码弹窗、H5 桥接页、按渠道分的支付方式图标。**删干净**，留着就是下一个人的坑。

### 验收

- [ ] 上面 10 个问题都有代码里的证据，不是推测
- [ ] 接法（并列/替换）已向用户复述并得到确认

---

## 2 · 第 1 步：凭据、开关与配置

### 要向网关管理员拿的东西

| 项 | 说明 |
|---|---|
| `app_id` | 不是秘密，可以进配置文件 |
| `app_secret` | **只显示一次**。**禁止**进代码库、进日志、进错误文案 |
| 网关根地址 | 管理员给的那串**原样填，末尾的路径段（如 `/api`）不能省** |
| 通知地址登记 | 把本项目的回调地址告诉管理员（也可以每次下单时显式传，见 §4 决策 D3） |

三个环境变量名由 SDK 固定：`JJPAY_BASE_URL` / `JJPAY_APP_ID` / `JJPAY_APP_SECRET`。

### 规则

- **必须**：`BaseURL` 连路径前缀一起填。网关按前缀分流，省掉就是 404。
  前缀**不参与签名**（v0.1.6 起），所以换域名、加减前缀、直连源站端口调试都不动签名。
- **必须**：客户端**进程启动时构造一次**并复用（`Client` 并发安全）。**禁止**每次下单
  新建一个。
- **必须**：把 SDK 的 `Logger` 接到项目的日志器上。SDK **只在重试前打一行**，那是唯一会被
  静默吞掉的事件——一次成功的调用背后可能藏着两次失败，不接上就永远查不出来。
- **必须**：区分"没配"和"配错"：
  - 三项凭据在配置文件与环境变量**两处都为空** → 本项目还没接这个渠道，**跳过初始化、
    不拦启动**，该渠道此后一律返回"未配置"错误。
  - 配了但配错（地址非法、缺密钥） → **当场返回错误让进程起不来**。把"密钥忘了配"
    留到第一次收款时才炸，代价太大。
  - ⚠️ 判"没配"时**必须**把环境变量也算进去。只看配置文件为空就 `return`，会把 SDK 的
    环境变量回退一并掐掉——而 `app_secret` 本来就该走环境变量。
- **决策 D1**：配置段放哪、叫什么，跟着项目既有支付配置的形状走；有 `Enable` 开关的项目
  沿用 `Enable`，没有的就以"凭据是否齐全"为开关。二选一，别造第三种。

### 验收

- [ ] `grep` 全仓，`app_secret` 只出现在配置读取处，不在任何字面量、日志、错误文案里
- [ ] 凭据全空时进程能正常启动，该渠道不可用；凭据配错时进程启动失败并指出哪一项

---

## 3 · 第 2 步：建一个适配包（全项目唯一 import SDK 的地方）

**必须**新建一个包（如 `pkg/jjpay` / `internal/paygw`），并让它成为**全项目唯一** `import`
上游 SDK 的文件。业务层需要的事件类型、事件名在这里以**类型别名**再导出。

理由有两条，都是实测出来的：上游换版本改结构时，改动面收在这一个包里；以及项目自己
往往也有一个叫 `pay` 的包，两个同名包在同一个文件里打架，别名能躲掉。

这个包的职责，**只有**这五件：

1. 建客户端、持有它、暴露 `Enabled()`
2. **金额换算**（元 ↔ 分），全项目只有这一处做换算
3. 调用 SDK 的六个接口，把 SDK 的错误**归一**成项目自己的错误
4. 验签（把 `http.Header` + 原始 body 交给 `(*Client).Verify`）
5. 把 SDK 的事件类型别名再导出

**禁止**在这个包里写业务规则（改订单状态、加余额、开通服务）。那些在 logic 层。

### 金额换算（**决策 D2**，最容易错账的一处）

- **禁止** `float64`。`19.90 * 100` 在 float 下是 `1989.999…`，`IntPart()` 直接截断成
  `1989` —— 少收一分钱，且没有任何告警。
- 项目用 `decimal` 存元时，`Mul(100)` 是精确的，问题只剩"多于两位小数怎么办"。**二选一**：

| 策略 | 做法 | 适合 |
|---|---|---|
| **严格** | 不是整数分 → **返回错误，一个字节都不发出去** | 库里就是 `decimal(10,2)`，出现三位小数说明上游算错了，应该炸 |
| **收敛** | `Round(0)` 四舍五入到分 | 计价规则本身会产生长小数（按天折算、按流量计费） |

  **禁止**静默截断（`IntPart()` 不带 `Round`）。悄悄少一分 = 实收与账面不是同一个数，
  且无处告警。选了哪一种，**在函数注释里写明理由**。
- **必须**：换算后校验 `minor > 0`。网关对 0 与负数一律返回 `10001`，在本地拦掉能省一次
  往返和一条看不懂的错误。

### 验收

- [ ] `grep -r "Star-Flex/jjpay-go" --include="*.go"` 只命中这一个包（测试文件除外）
- [ ] 换算函数有单测：0、负数、多余小数位、超大金额各一条

---

## 4 · 第 3 步：下单

### 字段映射

| SDK 字段 | 填什么 | 注意 |
|---|---|---|
| `OutTradeNo` | 项目自己的订单号 | 同时是幂等键 |
| `Subject` | **具体**买了什么 | 见 R5 |
| `TotalMinor` | 实际要收的钱（分） | 唯一真相；**退款永远按它退** |
| `Attach` | 自己的标记，写成 `key=value` | 原样回传在通知里，用来标用户/业务类型 |
| `OriginalMinor` / `DiscountLabel` | 原价与优惠行的名字 | 见 R6，**只是画面** |
| `Items` | 明细 | 见 R7，**拆不平宁可不传** |
| `ExpireMinutes` | 有效期 | 见 R4 |
| `NotifyURL` | 回调地址 | 见 D3 |
| `ReturnURL` | 回跳地址 | 见第 7 节，**必须公网 HTTPS 可达** |

### 规则

- **R1 幂等**：幂等键是 `(app_id, out_trade_no)`。同一订单号重复下单，**参数一致**返回
  **同一张单和同一个收银台链接**（`err == nil`）；**参数不一致**（尤其金额）返回
  `ErrOrderConflict`，网关**绝不**改动已有单。
  → 这条是前端"重复点击安全"的底层保证，第 8 节会用到。
  → 撞 `ErrOrderConflict` 说明**同一个订单号的金额变过**，那是项目自己的 bug，不是网络抖动。
- **R2 失败不落库**：**必须**——网关下单失败时，把错误**上抛**，**禁止**留下一张本地
  待支付单。那种单永远付不了，还会占住"有未支付订单"之类的闸（注销预检、限购），
  用户自己关不掉。
- **R3 顺序**：若项目是"先请求网关、后落库"，要意识到**用户秒付时，通知可能跑在落库之前**。
  第 5 节的处理必须能自愈（查不到本地单 → 返回错误让网关 15 秒后重投）。
- **R4 幂等命中一张死单**：重复下单命中的**可能是一张已被网关关闭的老单**（本地超时与
  网关有效期不一定同步收敛）。此时 `CheckoutURL` 是死链，用户点进去只看到"支付链接已失效"。
  **必须**判 `resp.Status == jjpay.OrderClosed` 并当场报错，**禁止**把死链发给用户。
  根治是让 `ExpireMinutes` 与本地超时对齐。
- **R5 标题**：收银台是独立一屏，不传明细时**只显示标题和一个金额**。
  `Subject` **必须**回答"买的是什么"（含规格/时长），**禁止**写成"XX 平台订单，用户ID: 123"
  ——用户对着一个数字掏钱。
  **禁止**把金额写进标题：收银台本来就单列了应付金额，同一个数出现两次。
  ⚠️ 拼标题时**逐个字段判零值**：升级单/差价单往往没有"时长"这类字段，无脑拼会拼出
  「…10M带宽 0个月」。
- **R6 原价**：`OriginalMinor` **必须 ≥ `TotalMinor`**，不满足**整单**返回 `10001`
  ——一笔本来能付的单在下单这一步就失败。
  **必须**加一道保险：`原价 ≤ 实付` 时**一律不传**。收银台少画一行"优惠 ¥0"，
  远比付不了强。
- **R7 明细**：带 `Items` 时 `Σ(UnitMinor × Qty)` **必须精确等于**原价（没传原价时等于
  `TotalMinor`），差一分都返回 `10001`。
  现实中常常拆不平：价格有多个来源（组件累加 / 固定价覆盖 / 按天折算），逐条取整到分
  又会与总价差几分。**决策 D4**：要么实现"能拆就拆、拆不平回退到不传"的兜底，要么
  **干脆不传 `Items`**（只传标题和金额，收银台照常工作）。**禁止**未经验算就传明细。
- **决策 D3 `NotifyURL` 传不传**：
  - **显式传**（推荐）：地址由本项目的配置决定，与网关后台无关；下单时会快照进订单，
    之后改配置不影响在途老单。选它就**必须**在启动时校验它非空——"收不到通知"等于"收不到钱"。
  - **不传**：走网关后台登记的那份，少一处配置漂移，但改地址要找管理员。

### 验收

- [ ] 下单失败时，本地没有留下任何待支付单（写一条用例）
- [ ] 同一订单号调用两次，拿到的是同一个 `CheckoutURL`
- [ ] 传了 `Items` 的话，有一条用例断言 `Σ(UnitMinor×Qty) == OriginalMinor`

---

## 5 · 第 4 步：通知端点（整个接入里最要命的一节）

这是"钱到了"的唯一真相。**禁止**用回跳、用前端轮询的结果去发货。

### 处理顺序（不许调换）

```
1. io.ReadAll(io.LimitReader(body, 256<<10))   ← 先限长读
2. client.Verify(header, rawBody)              ← 再验签（HMAC-SHA256 + ±5 分钟时间戳窗口）
3. 按 evt.Event 分发                            ← 验签通过之后才允许读任何字段
4. 回 200 + "SUCCESS"
```

- **必须**限长读。不设闸等于把"一个请求撑爆进程"的按钮放在公网上。256 KiB 对十几个
  字段绰绰有余。
- **禁止**在验签之前 `json.Unmarshal` body、**禁止**据此查库或打印业务字段。
  不验签的回调端点 = 任何人都能给你发"已支付"。
- **必须**把验签失败打进日志（路径、来源 IP、错误）。SDK 的中间件默认 `OnError == nil`
  ——验签失败只回 401，你这边什么都看不到；而"商户端点一直 401"在网关那侧的表现是
  重试 10 次后进死信，等你发现时钱已经收了一天。
- 用 gin/echo/fiber 时**禁止**用自由函数版的 `Middleware(secret)`：它要求你在 handler 侧
  再读一遍密钥，读空的后果是**每一条真实通知都被 401**。拿 `*Client` 的方法版。

### 发货前的三道闸（**必须**，一道都不能省）

项目里既有的支付宝/微信回调**往往一道都没有**，**禁止**照抄它们。

| 闸 | 检查 | 漏了会怎样 |
|---|---|---|
| 1 | 按 `evt.OutTradeNo` 找到本地单 | 找不到 → 返回错误让它重投（见 R3，可能是落库还没完成） |
| 2 | **本地单的渠道 == 本渠道** | 单号常常是全站自建号段、多渠道共用：一条合法的网关通知就能去结算一张走直连渠道下的单 |
| 3 | **通知金额 == 本地应付额** | 验签只证明"这条消息是网关发的"，不证明"它说的是这笔单该收的钱" |

**必须**把三道闸放在**任何业务动作之前**。业务函数常常会拿通知里的金额去加余额、去匹配
充值档位——等它跑完再发现不对，钱已经加过了。

### 幂等（SDK 帮不了你）

- 去重键：`pay.succeeded` / `order.closed` 用 `trade_no`（或本地单号）+ `event`；
  **退款事件一律用 `refund_no` + `event`**。
  ⚠️ 拿 `trade_no` 给退款事件去重，**同一支付单的第二笔退款会被当成重复投递丢掉**
  ——那是一笔真实发生、你却没入账的退款。一张表接多个项目的话，键里还要带项目标识。
- **必须**用**带条件的单条 UPDATE** 抢占，并检查 affected_rows：
  `UPDATE orders SET status='delivered' WHERE id=? AND status='paid'`。
  **禁止**"先查有没有处理过、再处理"的两步式写法——并发下两个请求都会查到"没处理过"，
  于是发两次货。
- **必须**把去重记录与业务更新放进**同一个事务**。先写"已处理"再开通服务，中途失败的话
  去重记录已经落下了，后面每一次重投都被挡在门外，而服务一次都没开通——收了钱没发货，
  且没有任何告警。
- **禁止**把 `RowsAffected == 0` 与 `err != nil` 混为一谈。数据库故障返回的是 `err`，
  把它当成"处理过了"回一个 SUCCESS，这条通知就**再也不会来了**。

### 应答决策表

回 `200` + body `SUCCESS`（大小写不敏感）= 收到了，别再投。其余一律算失败，按
`0/15/60/300/900/1800/3600/10800/21600/43200` 秒重试 10 次（约 24 小时）后进死信。

| 情形 | 回什么 | 为什么 |
|---|---|---|
| 处理成功 | `SUCCESS` | — |
| 本地单还没落库、数据库抖动 | **不回 SUCCESS** | 重投一次就能自愈 |
| 金额对不上、渠道对不上 | **不回 SUCCESS** | 结论不会变，但这是要人看的事故——进死信比悄悄咽下去强 |
| 退款终态事件（只需记录+告警） | `SUCCESS` | 重投一百次结论也一样，别浪费网关的重试 |
| **不认识的事件名** | `SUCCESS` | 重投 10 次之后你还是不认识，只会填满死信 |

### `order.closed`

把还挂着的待支付单置为已取消，**必须**用带 `status` 条件的单条 UPDATE。
通知迟到时（单其实已经付了）它一行都不会动，**禁止**无条件改状态——那会把已支付的单
改回取消。本地若有超时 cron 做同一件事，两边幂等，谁先到都对。

### 验收

- [ ] 伪造签名、重放旧时间戳、改金额、换密钥、截断签名、超长 body —— 全部被挡在 401
      （用 `jjpay.SignNotify` 造合法通知，再逐项篡改）
- [ ] 渠道不符 / 金额不符的通知，业务状态一行未变
- [ ] 同一条通知连投三次，只发一次货（**必须**跑在真数据库上，内存假 store 会让竞态静默消失）
- [ ] 迟到的 `order.closed` 不会把已支付单改回取消

---

## 6 · 第 5 步：退款

### **决策 D5：异步与同步的落差**

网关的退款是**异步**的：支付宝那侧通常同步就是终态，微信原路退回银行卡要 T+1~3 天，
同步返回只表示"网关收下了"。而很多项目的退款是**同步语义**（调完就地翻订单/订阅状态）。
这个落差**必须**显式取舍，三选一并写进 commit 正文：

| 口径 | 做法 | 代价 |
|---|---|---|
| **已受理即记账**（多数项目的现状） | 同步返回非失败就推进业务，真实终态靠通知兜 | `refund.failed` 时账已经记了，要人工回滚 |
| **终态才记账** | 本地退款单挂"处理中"，等 `refund.succeeded` 才翻状态 | 要新增一个中间态，改动面大 |
| **分渠道** | 同步终态的当场记，处理中的挂起 | 两条路都要维护 |

### 规则

- **必须**按 `resp.Status` 判，**禁止**用"调用没报错"当成退款成功：
  `RefundSucceeded` = 真退成功了，可以当场记账；`RefundProcessing` = 只收下了、结果未定；
  `RefundFailed` = 明确失败，**中止，不要记账**。
  （`RefundStatus` 是**整数**枚举，不是字符串，写 `switch` 时别当成字符串。）
- **必须**：写操作（下单/退款/关单）**SDK 一次都不重试**，所以超时/网络错误 **≠ 没退成**。
  正确姿势是**用同一个 `out_refund_no` 再调一次**（幂等），或**回查一次**确认。
  **禁止**让管理员"再点一次退款"时生成**新的** `out_refund_no` —— 那会在网关侧退出**第二笔钱**。
- 回查时，`ErrRefundNotFound`（60005）表示当初那次请求**根本没到**，这**不是失败**：
  按同一个 `out_refund_no` 重发是安全的。
- 超额退款返回 `60002`，`data.refundable_minor` 告诉你还能退多少，改参数重试即可。
  `r.TotalMinor - r.RefundedMinor` 就是余额，不必再查一次单。
- **必须**：退款按**实付**（`TotalMinor`）退。`OriginalMinor`（原价）与退款无关。

### 三个退款事件的处置

| 事件 | 含义 | **必须**做 | **禁止**做 |
|---|---|---|---|
| `refund.succeeded` | 钱到用户手里了 | 记一行（本地若挂着"处理中"就翻成成功） | — |
| `refund.failed` | 钱**退回了商户账户** | **告警给人**。本地若已按"退款成功"记过账，这里账实不符了 | 自动回滚订阅/服务——"用户这段时间还在用""余额要不要扣回"不是回调该替人做的决定 |
| `refund.stalled` | **不是终态**。钱已离开商户账户、还没到用户手里（只有微信会出现：退往原路时银行拒收） | 记下 `RefundNo` 与 `StalledReason`（渠道原话，直接给客服看）并告警 | **判定失败、给用户二次补偿**——那笔钱后面仍可能到账，补了就是退两次 |

告警文案**必须**把渠道原话原样带上，不改写、不归纳；`stalled` 的文案里**必须**写明
"未结束，勿二次补偿"。

### 并列接入时的铁律

**退款只能回原路。** 退款走哪条链路，只由**订单上记着的渠道**决定。
**禁止**在退款时重新走一遍"当前启用哪个渠道"的路由——那会把钱退到另一个商户号。
**必须**有一条回归护栏测试钉死这一点。

### 验收

- [ ] 口径（D5）已选定并写进 commit 正文
- [ ] 有用例：同步返回 processing / succeeded / failed 三档各走对分支
- [ ] 有用例：非业务错误后会用**同一个** `out_refund_no` 回查，而不是新建一笔
- [ ] 有用例：直连渠道的历史订单**不会**被改道到网关退款

---

## 7 · 第 6 步：回跳页与结果页

- **必须**：`ReturnURL` **公网 HTTPS 可达**。真实事故：某次接入把回跳地址配成了一台
  只服务 HTTP 的域名，443 直接握手失败（`unrecognized_name`），用户在微信里付完点"返回商户"
  ——点了、跳了、什么也没发生。**上线前用外网 curl 验一次那个地址**。
- **必须**：回跳**只准展示，不准发货**。回跳和通知是两条独立链路：用户付完关掉浏览器
  就没有回跳；旧链接被收藏/转发也不该再当凭证。
- 用 `VerifyReturn(r.URL.Query())` 验签过的结果可以直接画"支付成功 ¥19.90"，省掉一次查单。
  ⚠️ **`err != nil` 时 `res` 是 `nil`，必须 `return`**，往下走就是空指针。当作"没带结果"
  处理：拿单号去查单。
- **必须**：结果页**轮询查单**确认，**禁止**读 query 参数判成功。
- 长时间没收到通知的订单，**必须**有一条"定时查单"的兜底。查单接口自带惰性回源，
  查到的是真状态；但它是补偿，**禁止**当高频轮询打。

### 验收

- [ ] 外网能 `curl -I https://<return_url>` 拿到响应
- [ ] 结果页在 query 参数被篡改时不会显示成功

---

## 8 · 第 7 步：前端

渠道选择交给收银台之后，前端会**变简单**，主要工作是删代码。

**删掉**（替换式接入）：按 PC/移动端分的四路跳转、自绘的扫码弹窗、H5 桥接页、
`isMobile` 判断、按渠道分的支付方式图标。网关返回的**永远是一个跳转链接**。

**规则**：

- **必须**：新标签页要在**用户手势里同步** `window.open("", "_blank")` 先拿到，
  建单请求 `await` 回来之后再写 `tab.location.href`。等 `await` 回来才 `open`，
  手势已经消耗掉，会被浏览器的弹窗拦截器直接挡下。
  失败/异常路径上**必须** `tab.close()`，别在用户面前留一个空白页。
- 支付方式只剩一个选项时（例如"余额充值"不允许用余额付），**跳过**那一屏直接发起支付：
  一个单选项的页面没有任何可决策的东西。**必须**用一次性闸（`useRef`）防重复发起；
  就算真重复调了也安全——同一订单号在网关侧是幂等的，返回同一张单和同一个链接。
- **必须**：把"发起支付并把用户送到该去的地方"抽成**全站唯一出口**（一个 hook / 一个函数）。
  多个入口（订单页、详情页、充值弹窗）各写一份，必然漂移成"某个入口付完不跳转"这种
  极难复现的问题。
- 把支付方式的选择**前移到下单弹窗**时，可以在建单前做一次"余额够不够"的预检，避免
  留下一张必然付不掉的废单。预检**不是**扣款也**不**锁定渠道，真正的检查仍在支付那一步。
- "在线支付"这一项的图标：它既可能走微信也可能走支付宝，**挂任何单独一家的标都是误导**
  ——要么用中性图标，要么两家并排挂。
- 前端内部金额**一律传分（整数）**，只在渲染层除 100；金额与倒计时用
  `font-variant-numeric: tabular-nums`。

### 验收

- [ ] 在移动端真机上点一次支付，新标签/跳转没有被拦截
- [ ] 全站 `grep` 只有一个地方发起支付跳转

---

## 9 · 第 8 步：测试

**必须**有的几类（少一类就不算接完）：

1. **假网关 + 真签名 + 真 HTTP**：起一个 `httptest` 假网关，用 SDK 真的打过去。
   不要 mock 掉 `*Client`——签名与 HTTP 正是要验的那一层。
2. **对抗性通知**：用 `jjpay.SignNotify(secret, event, body, at)` 造一条合法通知，
   然后逐项篡改：伪造签名、换密钥、重放旧时间戳、改金额、改单号、截断签名、超长 body。
   **全部**必须被挡在 401 且业务状态一行未变。
3. **幂等**：同一条通知连投三次，只发一次货。**必须**跑在真数据库上。
4. **回归护栏**：直连渠道的历史订单不会被改道到网关（并列接入）。
5. **前缀场景**：让假服务端**自己成为剥前缀的反向代理**（`http.StripPrefix`），
   用带前缀的 `BaseURL` 走一遍下单/查单/关单。
   ⚠️ 这一条是真实事故的直接教训：商户与网关两端的测试**全是绿的**，因为两边的测试
   服务端都不剥前缀——**谁都没跑在生产的那个形状下**（附录 A-1）。
6. **金额边界**：0、负数、多余小数位、超大金额。

### 联调

- 网关有 **mock 渠道**：不需要真商户号、不花钱，点一下就模拟支付成功，走的是与真渠道
  **完全相同**的回调链路。找管理员给你的 App 打开它。
- 开发期 `notify_url` 若是内网地址，要请管理员在网关侧放行私有网段（默认禁止，防 SSRF）。

---

## 10 · 上线前检查表

```
[ ] app_secret 在环境变量里，不在代码库、日志、错误文案里
[ ] SDK 版本 >= v0.1.6
[ ] BaseURL 带着管理员给的路径前缀
[ ] 客户端进程启动时构造一次；Logger 已接到项目日志器
[ ] 未配置时进程能起、该渠道不可用；配错时进程起不来
[ ] 回调端点：限长读 → 验签 → 才读字段；验签失败有日志
[ ] 回调端点：本地单存在 / 渠道相符 / 金额相符，三道闸都在业务动作之前
[ ] 发货幂等：带条件的单条 UPDATE + 检查 affected_rows，与去重记录同事务
[ ] 退款事件用 refund_no 去重，不是 trade_no
[ ] 退款只回原渠道，有回归护栏测试
[ ] refund.failed / refund.stalled 会告警到人；stalled 文案写明"勿二次补偿"
[ ] 下单失败不留废单
[ ] 幂等命中已关闭的单时不把死链发给用户
[ ] return_url 外网 HTTPS 可达；结果页轮询查单，不信 query 参数
[ ] 有"长时间未收到通知就主动查单"的兜底
[ ] 金额全程是分（int64），换算只有一处，没有 float
[ ] 网关后台的通知地址已登记（或下单时显式传，且启动时校验非空）
```

---

## 附录 A · 真实踩过的坑（症状 → 成因 → 处置）

### A-1 每一次调用都 `20002 鉴权失败`

- **症状**：密钥、app_id 反复核对都没错，但所有接口都 20002。
- **成因**：SDK < v0.1.6 把 `BaseURL` 里的路径前缀也签进了 PATH，而网关按前缀分流、
  **剥掉前缀**才回源——网关背后的应用按 `/openapi/v1/...` 验签，两串对不上。
  **每一个带前缀接入的商户都会撞上**，不是某一家的配置问题。
- **处置**：升到 **v0.1.6+**（签名只覆盖两端都认识的那一段，前缀与 host 同属"投递地址"，
  两者都不签）。
- **一般化的教训**：**禁止**把"会在中途被合法改写的信息"签进签名。method、body、
  自定义头是端到端不变的；host 与路径前缀是投递地址，改写它正是反向代理的本职。
- **对照实验法**（排查任何 20002 都适用）：拿一个**必然不存在**的单号去查单（只读、
  不产生数据）。回 `20002` = 验签这一关就没过；回 `30002 支付单不存在` = 验签过了，
  问题在别处。

### A-2 幂等命中了一张死单，用户点开是"支付链接已失效"

- **成因**：重复下单返回的是**已存在**的那张单，而它已被网关按有效期关掉；本地超时与
  网关有效期没有对齐。
- **处置**：下单后判 `resp.Status == OrderClosed` 就地报错；让 `ExpireMinutes` 与本地超时对齐。

### A-3 下单被整单拒绝 `10001`

三个高频原因，按顺序排查：

1. `OriginalMinor < TotalMinor`（原价小于实付）→ 加"不满足就不传"的保险。
2. 带了 `Items` 但 `Σ(UnitMinor×Qty) ≠ 原价`（逐条取整到分的误差、按天折算的单）→ 拆不平就别传。
3. 金额 ≤ 0、超上限（单个金额 ≤ 1 亿元、`Qty` ≤ 10000、`Items` ≤ 100 行、
   `Item.Name` ≤ 64 字符）。

### A-4 收银台标题写着「… 0个月」

- **成因**：无脑给所有订单拼"%d个月"，而升级/差价单根本没有"时长"字段（它按剩余天数折价）。
- **处置**：拼标题时逐字段判零值。**一般化**：凡是拼给用户看的串，每个字段都要问一句
  "这类订单有没有这个字段"。

### A-5 用户在微信里付完，点"返回商户"什么也没发生

- **成因**：`return_url` 那个域名的 443 不服务（只有 HTTP），浏览器握手直接失败。
- **处置**：上线前外网验一次。**这条对所有渠道都成立**，只是支付宝那条路会先落在
  结果页上、没人盯着那行倒计时，所以只在微信里显形。

### A-6 前端点"去支付"，新标签页被拦截器挡下

- **成因**：等建单请求 `await` 回来才 `window.open`，用户手势已经消耗掉。
- **处置**：点击事件里**同步**预开一个空白标签，拿到句柄，`await` 之后写 `location.href`。

### A-7 回调端点一直 401，钱收了一天才发现

- **成因**：验签失败没有任何日志（SDK 中间件的 `OnError` 默认是 `nil`），或者用了自由
  函数版 `Middleware(secret)` 而那侧把密钥读空了。
- **处置**：接上 `OnError`；用 `*Client` 的方法版。网关侧的表现是重试 10 次后进死信。

### A-8 "调用没报错"被当成退款成功

- **成因**：只判 `err == nil`，没判 `Status`。
- **处置**：按 `Status` 三分支判。另：**超时不等于没退**——写操作 SDK 一次都不重试，
  要用**同一个** `out_refund_no` 回查确认，否则管理员重试一次就在网关侧退出第二笔钱。

### A-9 两端测试全绿、生产全红

- **成因**：商户的测试服务端与网关的测试服务端**都不是生产的那个形状**（不剥前缀、
  不带网关）。
- **处置**：让测试里的假服务端自己成为网关（剥前缀的反向代理）。
- **一般化**：**跨组件的隐式契约，必须有一条测试跑在"生产的那个形状"下**。

---

## 附录 B · 错误码速查（完整表见 README）

| 码 | 哨兵 | 含义 | 怎么办 |
|---|---|---|---|
| 10001 | `ErrParamInvalid` | 参数非法 | 看 `data` 里的字段级说明；见 A-3 |
| 10002 | `ErrServerBusy` | 系统繁忙 | 读操作 SDK 已自动重试 |
| 20002 | `ErrSignMismatch` | 鉴权失败 | 见 A-1 |
| 20003 | `ErrTimestampSkew` | 时间戳超窗 | **你的服务器时钟没校准**，先看 NTP |
| 20005 | `ErrAppDisabled` | App 被停用 | 联系网关管理员 |
| 30001 | `ErrOrderConflict` | 单号重复且参数不一致 | **你的 bug**：同一单号金额变过 |
| 30002 | `ErrOrderNotFound` | 查无此单 | 确认单号；也可用于 A-1 的对照实验 |
| 60002 | `ErrRefundExceedTotal` | 退款超额 | 看 `data.refundable_minor` |
| 60005 | `ErrRefundNotFound` | 查无此退款单 | **不是失败**：当初请求没到，重发安全 |

另外三类非业务错误：`*HTTPError`（非 200，说明没走到应用层：网关 502、路径写错）、
`ErrResponseSign`（响应验签失败 → **当"不知道"处理，去查单**，不是"失败"）、
`ErrTimestampWindow`（时钟没校准）。

---

## 附录 C · 提交切分建议

一次典型接入拆成这几个 commit，每个都能单独回滚：

1. 适配包 + 配置段 + 开关（默认关闭）——此时对既有逻辑零影响
2. 下单链路（含"失败不落库"）
3. 回调端点 + 三道闸 + 幂等（**这个 commit 必写正文**：边界条件与并发假设）
4. 退款链路 + 告警（**必写正文**：D5 选了哪个口径、为什么）
5. 前端（替换式接入时**必须**与后端收窄枚举的那个 commit 合并成同一个）
6. 文档：把本项目的开关、配置项、渠道值写进项目自己的 README

涉及钱的 commit **必须**写正文，说明改了什么规则、并发与退款的边界怎么考虑的——
那是逐行人工审查的入口。
