# 现有支付系统接入指南

本文适用于已有订单、支付和退款逻辑的项目。API 调用示例见 [README.md](README.md)；本指南介绍接入前需要确认的事项、业务处理要求和验收方法。

示例中的域名、应用标识和订单号均为占位内容。实际配置应由部署环境提供，密钥不得写入源码、日志或公开的问题报告。

## 1. 确认接入范围

开始修改前，先根据现有代码确认以下事项，并记录方案及必要的业务决策。

| 事项 | 需要确认的内容 |
|---|---|
| 接入方式 | 新增 jjpay 支付渠道，还是替换现有直连渠道 |
| 金额单位 | 数据库存储的是元还是分，使用整数、精确十进制还是浮点类型 |
| 订单编号 | 商户订单号是否唯一、不复用；多个支付来源是否共用编号 |
| 退款流程 | 是否已有“处理中”状态，同步接口返回后如何记账 |
| 通知处理 | 是否核对订单、支付来源、金额和币种，是否支持幂等处理 |
| 订单占用 | 待支付订单是否占用库存、限购名额或其他业务资源 |
| 支付页面 | 哪些入口负责跳转、展示二维码或区分终端与渠道 |
| 超时设置 | 本地订单有效期与网关 `ExpireMinutes` 是否一致 |
| 结果补偿 | 通知未送达时是否会主动查单 |
| 异常跟进 | 退款失败、退款停滞和通知投递失败由谁处理 |

### 新增支付渠道

新增渠道与既有渠道并行运行时，需要保留历史订单的支付来源。退款、查单和对账均按订单记录的原渠道处理，不能根据当前启用的渠道重新选择。

新渠道可默认关闭，完成配置和联调后再逐步启用。未配置时不应影响原有支付流程。增加回归测试，确认历史直连订单不会被转到 jjpay 退款。

### 替换直连渠道

新订单可以统一进入 jjpay 收银台，但历史订单的退款、查单和通知处理仍需保留，直到相关业务结束。前后端涉及渠道枚举和请求参数的变化应配套交付，避免新后端无法识别旧前端的请求。

迁移未支付订单前，应确认旧渠道的支付尝试是否仍可能成功，再决定关闭旧尝试或创建新支付单。不要直接覆盖仍在支付中的订单来源。

收银台已负责支付方式选择和终端适配。确认旧流程不再需要后，可移除对应的二维码弹窗、H5 桥接页和跳转分支。

### 验收

- [ ] 已根据代码确认上述事项，并记录接入范围。
- [ ] 已明确历史订单和在途支付的处理方式。
- [ ] 已记录金额换算、通知地址和退款记账等业务决策。

## 2. 配置客户端

使用 `v0.1.6` 或更高版本。旧版本对 `BaseURL` 路径前缀的签名处理不同，经过剥除前缀的反向代理时可能出现 `20002`。升级时请确认服务端兼容性，详见 [CHANGELOG.md](CHANGELOG.md)。

| 配置 | 说明 |
|---|---|
| `JJPAY_BASE_URL` | 管理员提供的完整网关地址，保留 `/api` 等路径前缀 |
| `JJPAY_APP_ID` | 应用标识 |
| `JJPAY_APP_SECRET` | 应用密钥，通过环境变量或密钥管理服务提供 |
| 通知地址 | 在后台登记，或由每次下单的 `NotifyURL` 指定 |

在进程启动时创建并复用 `Client`，将 `Logger` 接入项目日志系统，以便排查请求重试。

可选渠道应区分“未配置”和“配置错误”：全部配置为空时，可跳过初始化并将渠道标为不可用；已启用但缺少密钥、地址格式错误等情况，应在初始化时报告。判断配置是否为空时，需要同时考虑配置文件和 SDK 支持的环境变量回退。

开关和配置结构沿用项目现有约定。避免在多个处理函数中重复读取密钥。

### 验收

- [ ] 未配置时原有业务正常运行，启用但配置错误时能及时发现。
- [ ] 客户端已复用，重试日志和通知验签失败日志可查询。
- [ ] 已检查源码、示例、日志和错误信息，不包含真实密钥。

## 3. 封装调用与金额换算

建议在一个适配包中集中管理客户端、配置、金额换算和错误映射。订单状态更新、余额变更、发货等业务逻辑仍由项目的业务层处理。

SDK 使用 `int64` 表示币种最小单位，CNY 为分。若原系统以元存储，使用整数或精确十进制完成换算，不要通过浮点运算后直接截断取整。

对于换算后不足一分的金额，应明确采用哪种策略：

| 策略 | 做法 | 适用情况 |
|---|---|---|
| 拒绝额外小数位 | 换算后不是整数分则返回错误 | 业务金额本应最多两位小数 |
| 按规则舍入 | 依据计价规则四舍五入或分摊到分 | 按天、按量计费等会产生额外小数位的场景 |

换算结果必须大于 0，并检查范围和溢出。换算规则应集中实现，在注释中说明依据。

### 验收

- [ ] 金额换算只有一套实现，精度规则明确。
- [ ] 已覆盖零、负数、额外小数位和超大金额。
- [ ] 适配包不直接承担订单状态和发货逻辑。

## 4. 创建支付单

| SDK 字段 | 业务含义 | 注意事项 |
|---|---|---|
| `OutTradeNo` | 商户订单号 | 在当前应用内唯一，也是幂等键 |
| `Subject` | 商品或服务名称 | 可包含规格和服务期限，不应包含客户隐私 |
| `TotalMinor` | 最终应付金额 | CNY 单位为分，优惠应由业务系统计算完成 |
| `Attach` | 业务关联标记 | 随通知原样返回，不要放入密钥或个人敏感信息 |
| `OriginalMinor` / `DiscountLabel` | 原价和优惠名称 | 用于展示，不改变实际支付金额 |
| `Items` | 商品明细 | 明细合计必须与原价或应付金额一致 |
| `ExpireMinutes` | 支付有效期 | 与本地订单超时规则协调 |
| `NotifyURL` | 异步通知地址 | 可覆盖后台配置，创建订单时保存快照 |
| `ReturnURL` | 支付后的回跳地址 | 生产环境使用可从外网访问的 HTTPS 地址 |

### 幂等与失败处理

同一 `OutTradeNo`、相同参数重复下单时，网关返回已有订单及相同的 `CheckoutURL`。金额、标题或通知地址不一致时返回 `ErrOrderConflict`，不会修改原订单。

检查返回订单的 `Status`。如果是 `OrderClosed`，应提示订单已关闭，按业务规则重新发起支付，不再提供已失效的支付链接。调整 `ExpireMinutes` 可以减少两端超时不一致的问题，但不会重新打开已关闭订单。

明确失败与结果未知需要分别处理。参数校验等明确失败，应释放或取消本地的待支付占用；网络超时无法证明网关未建单，应保留可追踪状态，通过查单确认，或使用相同单号和参数重试。

若采用先请求网关、后保存本地订单的流程，通知可能先于本地落库到达。通知处理应允许暂时返回失败，等待后续重投；同时避免留下无法关联的网关订单。

### 标题、优惠与明细

标题应描述购买内容，按订单类型选择有效字段，避免出现“0 个月”等无意义信息。金额由收银台单独显示，无需重复写入标题。

`OriginalMinor` 为 0 或等于 `TotalMinor` 时表示无优惠；其他非零值必须大于 `TotalMinor`。传入明细时，`Σ(UnitMinor × Qty)` 必须等于原价；未设置原价时等于应付金额。`Qty` 省略或小于等于 0 时按 1 计算。

若业务无法合理拆分明细，可以只传标题和金额。不要提交未经核对的明细，也不要通过修改实际应付金额来匹配展示数据。

### 通知地址

可以由业务系统显式传入 `NotifyURL`，也可以使用网关后台登记的地址。选用显式配置时，应在启动或启用渠道时校验地址；选用后台配置时，应确认管理员已登记正确地址。地址在创建订单时保存，后续配置变更不影响已有订单。

### 验收

- [ ] 重复下单返回相同订单，参数冲突能正确提示。
- [ ] 已关闭订单不会继续展示支付链接。
- [ ] 明确失败不会留下无法释放的业务占用，超时订单可查询确认。
- [ ] 商品明细、原价和应付金额符合校验规则。

## 5. 接收通知

### 验签顺序

1. 限制请求体大小，SDK 中间件的上限为 256 KiB。
2. 验证请求头、时间戳和原始请求体的签名。
3. 验签通过后解析并处理业务字段。
4. 完成处理后返回成功应答。

使用 SDK 中间件时，通过 `OnError` 记录验签失败。自行适配框架时，使用 `http.MaxBytesReader` 等机制拒绝超长请求，再调用 `(*Client).Verify`。不要在验签前根据请求内容更新订单，也不要将密钥和完整通知载荷写入日志。

### 业务校验

处理支付成功通知前，需要确认：

| 检查项 | 处理要求 |
|---|---|
| 本地订单存在 | 按商户订单号查找；暂未落库时返回失败以便重投 |
| 应用和支付来源匹配 | 防止多个渠道共用订单号时错误处理其他来源的订单 |
| 金额与币种一致 | 与本地订单记录核对，异常时不执行发货或充值 |

退款事件还应核对对应的本地退款单、原支付单和退款金额。对重复事件也应先确认关联关系有效。

### 幂等与事务

支付成功和订单关闭事件按 `trade_no + event` 去重，退款事件按 `refund_no + event` 去重。多个应用共用事件表时增加应用标识。

用唯一约束或带状态条件的单条更新处理并发，避免“先查是否处理过，再单独更新”的竞态。去重记录与业务更新必须在同一事务中提交，业务失败时一起回滚。

数据库返回错误时，应先处理错误，再判断受影响行数；不能将数据库故障视为已经处理过通知。涉及外部系统的操作，可在事务内写入 outbox，再异步执行。

### 成功应答

成功应答为 HTTP 200，正文为 `SUCCESS`（大小写不敏感，允许前后空白），或 JSON `{"code":"SUCCESS"}`。其他应答触发重试。投递间隔为 `0/15/60/300/900/1800/3600/10800/21600/43200` 秒，共 10 次，约 24 小时；仍失败则进入死信队列。

| 情形 | 应答与处理 |
|---|---|
| 处理成功或已确认处理过 | 返回 `SUCCESS` |
| 本地订单暂未落库、数据库故障 | 返回失败，等待重投 |
| 应用、支付来源或金额不匹配 | 不执行业务，不返回 `SUCCESS`，记录异常并告警 |
| 退款终态事件 | 完成本地结果记录及必要的告警任务落库后，返回 `SUCCESS` |
| 未识别的事件类型 | 记录事件类型以便排查；确认无需处理后返回 `SUCCESS`，避免无效重试 |

处理 `order.closed` 时，只能关闭符合预期状态的待支付订单。使用带状态条件的更新，防止迟到通知覆盖已经确认的支付结果。

### 验收

- [ ] 伪造签名、错误密钥、过期时间戳、篡改正文、截断签名和超长请求均被拒绝。
- [ ] 验签通过但订单、来源或金额不符时，不发生业务更新。
- [ ] 串行和并发重复投递只执行一次业务，使用真实数据库验证。
- [ ] 业务事务失败后，重投仍能继续处理。
- [ ] 迟到的关单通知不改变已支付订单。

## 6. 处理退款

退款接口可能同步返回最终结果，也可能返回处理中。请根据 `Status` 判断，不能仅根据 `err == nil` 将退款记为成功。

| 状态或事件 | 含义 | 处理方式 |
|---|---|---|
| `RefundSucceeded` / `refund.succeeded` | 退款成功 | 更新本地退款状态，幂等完成记账 |
| `RefundProcessing` | 退款处理中 | 保留处理中状态，等待通知或主动查询 |
| `RefundFailed` / `refund.failed` | 退款失败 | 记录原因，通知负责人员，按业务规则处理 |
| `refund.stalled` | 尚未结束，需要人工跟进 | 保留处理中状态，记录 `RefundNo`、`StalledReason` 并告警 |

`RefundStatus` 是整数枚举。同步成功后仍会收到终态通知，应由同一套幂等逻辑处理。

### 业务记账

推荐在确认成功后记为退款完成。若业务需要在受理时先释放服务或调整权益，应将这些动作与“退款成功”区分记录，并明确失败后的补偿流程。同步响应已确认成功时可以立即记账，处于处理中时继续等待结果。

`refund.stalled` 不表示失败。跟进记录应注明“退款尚未结束，请勿重复退款或补偿”。渠道返回的原因保留在有访问控制的记录中，对外展示时注意其中可能包含的敏感内容。

### 重试、额度与原路退款

- 退款不自动重试。网络超时后，使用 `QueryRefundByOutRefundNo` 查询，或以相同 `OutRefundNo` 和参数重试；不要生成新的退款单号。
- `ErrRefundNotFound` 表示当前未查到退款单，不能据此断言原请求从未到达。仍需沿用原商户退款单号，避免重复退款。
- 超额退款返回 `60002`，响应的 `data.refundable_minor` 可用于了解剩余额度；最终可退金额以服务端校验为准。
- 退款以原支付单的 `TotalMinor` 为依据，与展示用的 `OriginalMinor` 无关。
- 历史订单继续通过原支付来源退款。当前渠道配置的变化不能改变历史订单的退款路径。

### 验收

- [ ] 已明确处理中、成功、失败对应的业务记账方式。
- [ ] 同步响应和异步通知重复到达时，不重复记账。
- [ ] 超时后沿用同一商户退款单号查询或重试。
- [ ] 历史订单退款仍走原渠道。
- [ ] 退款失败和停滞均有可追踪的处理记录。

## 7. 回跳页和前端

生产环境的 `ReturnURL` 应具有有效 HTTPS 证书，并可从外网访问。上线前确认域名、端口和证书配置正确。

服务端调用 `VerifyReturn` 验签通过后，可以直接展示回跳结果。返回错误时结果为 `nil`，应停止读取结果字段，改由服务端查询当前用户有权访问的订单。前端不能直接相信未经验证的 URL 参数。

回跳只用于展示，发货以异步通知或服务端查单为依据。长时间没有收到通知的订单，应通过定时查单补偿；查询可能触发渠道请求，需要控制频率。

前端统一跳转到 `CheckoutURL`。若需要在新标签页支付，在点击事件中同步打开空白页，再在建单完成后设置地址；处理弹窗被拦截的情况，并在建单失败时关闭空白页。

多个支付入口应复用跳转逻辑，防止重复发起请求。只有一种支付方式时，可简化选择步骤。余额预检仅用于提示，最终校验仍由后端完成。

金额在传输和计算时使用整数分，展示时再转换为元。支付入口可使用“在线支付”等中性标识，由收银台展示具体渠道。

### 验收

- [ ] 回跳地址在外网可达，HTTPS 证书有效。
- [ ] 篡改 URL 参数不能伪造支付结果。
- [ ] 关闭浏览器不影响通知处理和业务完成。
- [ ] 已在移动端验证跳转、弹窗拦截和失败后的处理。

## 8. 测试与上线检查

接入测试应覆盖真实 HTTP 请求、签名校验和数据库并发处理：

1. 使用 `httptest` 模拟网关，由真实 SDK 发起请求并验证签名。
2. 使用 `SignNotify` 生成通知，覆盖合法、篡改、过期、超长和重复通知。
3. 在真实数据库上验证幂等、并发、事务回滚和重投。
4. 验证历史订单的查单及退款路径。
5. 模拟生产反向代理的路径处理，例如用 `http.StripPrefix` 剥除前缀。
6. 验证金额边界、退款状态以及下单超时后的恢复。

可联系管理员为测试应用开启 mock 渠道，验证收银台到异步通知的完整流程。开发通知地址位于私网时，需按网关的开发环境配置联调；不要为此修改生产环境的地址校验策略。

上线前检查：

- [ ] 配置与密钥按环境提供，示例和问题报告已脱敏。
- [ ] SDK 与网关版本兼容，`BaseURL` 路径前缀正确。
- [ ] 通知地址已配置，验签失败和处理失败均可追踪。
- [ ] 订单核对、金额核对、事务与幂等已验证。
- [ ] 下单失败及结果未知时，本地订单均有后续处理路径。
- [ ] 退款始终关联原支付来源，失败和停滞可及时跟进。
- [ ] 回跳仅用于展示，未收到通知时有查单补偿。
- [ ] 已验证实际代理路径和移动端支付流程。

## 常见问题

| 现象 | 排查方向 |
|---|---|
| 所有接口返回 `20002` | 检查应用与密钥是否匹配，再核对 SDK 版本、路径前缀及代理处理方式 |
| 重复下单后链接失效 | 检查返回状态是否为 `OrderClosed`，以及两端订单有效期 |
| 下单返回 `10001` | 检查金额范围、原价与实付关系、明细合计和字段长度 |
| 商品标题出现“0 个月” | 检查该订单类型是否有服务期限，只拼接有效字段 |
| 支付后无法返回业务页面 | 检查回跳域名的 HTTPS 证书、端口和外网可达性 |
| 新标签页被拦截 | 检查是否在异步请求完成后才调用 `window.open` |
| 通知持续返回 401 | 检查 `OnError` 日志、应用密钥、服务器时间和请求体是否被修改 |
| 退款调用成功但未完成 | 检查 `Status`，通过通知或查退款确认最终结果 |
| 本地测试通过，线上验签失败 | 检查测试是否覆盖生产环境的反向代理及路径前缀 |

金额相关限制：单个金额不超过 1 亿元，`Qty` 不超过 10000，`Items` 不超过 100 行，`Item.Name` 不超过 64 字符。

### 常见错误码

完整错误定义见 [errors.go](errors.go)。

| 错误码 | 预定义错误 | 含义与处理 |
|---|---|---|
| 10001 | `ErrParamInvalid` | 参数不合法，检查响应中的字段说明 |
| 10002 | `ErrServerBusy` | 系统繁忙，读操作由 SDK 有限重试 |
| 20002 | `ErrSignMismatch` | 签名不匹配，检查凭据和签名路径 |
| 20003 | `ErrTimestampSkew` | 时间戳超窗，检查时钟同步和请求延迟 |
| 20005 | `ErrAppDisabled` | 应用已停用，联系管理员 |
| 30001 | `ErrOrderConflict` | 同一单号的金额、标题或通知地址不一致 |
| 30002 | `ErrOrderNotFound` | 未查到支付单，核对应用和单号 |
| 60002 | `ErrRefundExceedTotal` | 退款超额，检查剩余可退金额 |
| 60005 | `ErrRefundNotFound` | 当前未查到退款单，核对单号并沿用原单号处理 |

`*HTTPError` 表示 HTTP 状态码不是 200；`ErrResponseSign` 表示响应无法通过验签，业务结果需进一步查询；`ErrTimestampWindow` 表示验签时间戳超出允许范围。
